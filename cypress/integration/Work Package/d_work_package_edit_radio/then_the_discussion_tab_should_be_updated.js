import { Then } from 'cypress-cucumber-preprocessor/steps';

Then('the discussion tab should updated with a comment by the current user saying {string}', function(comment) {
  //TODO - Waiting on https://toponify.atlassian.net/browse/TOP-572
});
